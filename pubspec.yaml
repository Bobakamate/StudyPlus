name: manform
description: "Online courses application."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.3
  provider: ^6.1.2
  fluttertoast: ^8.2.4
  shared_preferences: ^2.2.3
  file_picker: ^4.0.0
  permission_handler: ^10.4.5
  flutter_local_notifications: ^9.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true
  assets:
    - assets/images/
